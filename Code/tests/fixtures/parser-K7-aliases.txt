a.ts (Replace: foo)
```ts
function foo(){ return 1; }
```

b.ts (Update: bar)
```ts
function bar(){ return 2; }
```

c.ts (Add after: imports)
```ts
import x from './x';
```

d.ts (Add: baz)
```ts
const baz = 1;
```

e.ts (Add before: qux)
```ts
const qux = 1;
```

f.ts (Remove: gone)
```
